# Unreal Engine Modes Modeling

- The `Create` palette can be used to create various shapes
- There's also more shapes available (like `Plane`) under the `Add > Shapes` (Add is `+` over cube in the toolbar) 
